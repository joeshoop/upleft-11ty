{
  "name": "hella-simple-eleventy-tailwind-starter",
  "description": "Hella Simple Eleventy + Tailwind Starter",
  "scripts": {
    "serve": "rm -rf _site && npx @11ty/eleventy --serve --quiet & npx tailwindcss -i ./src/_css/main.css -o ./_site/styles.css --watch",
    "build": "rm -rf _site && ELEVENTY_ENV=production npx @11ty/eleventy && npx tailwindcss -i ./src/_css/main.css -o ./_site/styles.css --minify"
  },
  "keywords": [],
  "author": "Marcel Appelman",
  "license": "MIT",
  "browserslist": "defaults, not ie <= 11",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@11ty/eleventy-navigation": "^0.3.5",
    "tailwindcss": "^3.3.6"
  },
  "dependencies": {
    "eleventy-plugin-backlinks": "^0.2.1"
  }
}
